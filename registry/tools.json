{
  "version": 1,
  "description": "Optional registry of MCP servers and CLI tools the agent may use. Extend as you wire more.",
  "mcps": [
    {
      "id": "cursor-ide-browser",
      "purpose": "Headless/IDE browser: navigate, snapshot, QA flows",
      "notes": "Always read server INSTRUCTIONS; snapshot after navigation."
    },
    {
      "id": "cursor-app-control",
      "purpose": "Create project dirs + git init; move agent workspace root"
    },
    {
      "id": "n8n-mcp",
      "path": "tools/n8n-mcp/index.js",
      "purpose": "n8n workflow CRUD + execution from Claude Code — list, get, create, update, activate, execute, poll executions",
      "env_vars": ["N8N_BASE_URL", "N8N_API_KEY"],
      "tools": ["list_workflows", "get_workflow", "create_workflow", "update_workflow", "activate_workflow", "deactivate_workflow", "execute_workflow", "get_executions", "get_execution", "delete_workflow"],
      "setup": "cd tools/n8n-mcp && npm install",
      "claude_code_config": {
        "command": "node",
        "args": ["tools/n8n-mcp/index.js"],
        "env": { "N8N_BASE_URL": "${N8N_BASE_URL}", "N8N_API_KEY": "${N8N_API_KEY}" }
      }
    }
  ],
  "cli": [
    {
      "id": "gh",
      "purpose": "GitHub: repos, issues, PRs, API"
    },
    {
      "id": "youtube-top-comments",
      "path": "tools/youtube-top-comments",
      "purpose": "Export YouTube top comments (yt-dlp) to JSON/CSV/MD; agent entry `tools/youtube-top-comments/AGENTS.md`"
    },
    {
      "id": "competitive-site-research",
      "path": "tools/competitive-site-research",
      "purpose": "Sitemap crawl → Obsidian vault (meta/hubs/pages/articles) + UX audit doc pattern; skill `.cursor/skills/competitive-site-research/`; persona `prompts/competitive-research-agent.md`"
    }
  ]
}
