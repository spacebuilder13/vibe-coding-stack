# Bundled tools

Small CLIs vendored into this stack so one clone carries them.

| Tool | Purpose |
|------|---------|
| [youtube-top-comments](./youtube-top-comments/) | YouTube top comments → JSON / CSV / MD — start at `youtube-top-comments/AGENTS.md` |

Register new tools in `registry/tools.json` and mention them in root `AGENTS.md`.
